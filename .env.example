DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

FONNTE_TOKEN=
WAREHOUSE_PHONE=
CRON_SECRET=
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_KOPERASI_NAME=KUD Sumber Makmur

# Push Notifications (VAPID Keys)
# Generate with: node -e "const webpush = require('web-push'); const k = webpush.generateVAPIDKeys(); console.log(k.publicKey, k.privateKey)"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:notify@kud-transparansi.id
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
